const r=document.getElementById('refresh');if(r)r.onclick=()=>{document.getElementById('temp').textContent=(33.5+Math.random()*2).toFixed(1)+'°C';document.getElementById('hum').textContent=Math.round(54+Math.random()*9)+'%';document.getElementById('act').textContent=Math.round(74+Math.random()*20);document.getElementById('weight').textContent=(40.8+Math.random()*2).toFixed(1)+' kg';r.textContent='✓ Updated';setTimeout(()=>r.textContent='↻ Refresh Readings',1000)};
const vf=document.getElementById('verifyForm');if(vf)vf.onsubmit=e=>{e.preventDefault();document.getElementById('message').textContent=document.getElementById('batch').value.trim().toUpperCase()==='HC-2026-041'?'✓ Batch found — verified demo record displayed.':'Try demo ID HC-2026-041.'};
const cf=document.getElementById('contactForm');if(cf)cf.onsubmit=e=>{e.preventDefault();document.getElementById('contactMsg').textContent='✓ Demo message captured. Connect a backend/email service for real submissions.';cf.reset()};
